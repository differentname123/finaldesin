把服务端功能分成三个部分，分别用线程跑起来分别是：
	1.文件的接收，就一直监听接收pcm音频文件，然后保存到相应文件夹下
	2.pcm转字符串，也用一个线程来处理这个功能，以文件名来作为最近处理到的文件
	3.关键字的提取，也是分别监测，只要没有新的文件来就等待
	4.进行一个分析功能实现，主要是对（一段时间内）关键字词语（每个词性应该都要有）的频率进行总结排序

今日总结数据分析：
	词性个数统计
	最多话的时间
	最长安静时间段
	最长的句子
	总词语数
	说得最多的词语

词云：
	build_word_cloud(List wordFrequencies,int chang,int kuan,String backstyle)
		参数说明：传入关键词及其频率（list格式），在传入图片分辨率，chang和kuan，最后以backstyle所指向的图片作为形状